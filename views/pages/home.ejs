<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FilesPool</title>
</head>

<body>
  <h1>FilesPool</h1>

  <p>Hello there, this is list of files that you can download</p>

  <a href="<%= filesZipUrl %>">Or Download All as Zip</a>
  <ul>
    <% files.forEach(function(file) { %>
    <li><a href="<%= file.url %>"><%= file.name %></a></li>
    <% }) %> <% if (subFolder) { %>
    <br />
    <a href="/">Back to home</a>
    <% } %>
  </ul>
</body>

</html>